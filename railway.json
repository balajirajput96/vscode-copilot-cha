{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix ./web-app/server && npm install --prefix ./web-app/client && npm run build --prefix ./web-app/client"
  },
  "deploy": {
    "startCommand": "npm start --prefix ./web-app/server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}